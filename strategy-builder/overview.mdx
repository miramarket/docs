---
title: "Strategy builder overview"
description: "Learn how the Miramarket workspace and strategy canvas fit together."
---

The live strategy builder is available at [app.miramarket.org](https://app.miramarket.org). It lets you create a strategy visually for Polymarket, the currently supported venue. Choose nodes in the left panel, then click to add them or drag them onto the canvas.

<img src="/images/tutorial/builder-overview.png" alt="Miramarket strategy builder with the top bar, market panel, and canvas shown" />

## Main areas

| Area | What it does |
| --- | --- |
| Top bar | Starts a new draft, shares a link, runs a simulation, opens run options, and signs you in. |
| Left panel | Switch between **Condition** and **Market** to find nodes. |
| Canvas | Shows the nodes and their order in the strategy. |

## Nodes on the canvas

Every strategy starts with a node. A condition checks a rule. A market action buys a selected outcome. **Hold** keeps the current position open. **Wallet** directs funds to the selected wallet destination.

Click a palette card to add it relative to the node you selected. Dragging is useful when you want to place a node before, after, or below another node. The canvas arranges the resulting path for you; you do not need to draw connectors manually.

## A simple strategy

Start with one condition and one market action. For example:

> If the selected outcome is above `50¢`, buy its `YES` side.

One action receives a `100%` allocation by default. When a condition has more than one action, make sure the allocations add up to `100%`.

## Learn every node

Use [Strategy builder nodes](/strategy-builder/nodes) for a complete reference of Price Condition, Time Condition, Resolution, Buy Market, Wallet, and Hold. It also explains how conditions, actions, and allocations fit together.

## Build safely

**Run Live** is available. Use **Simulate** first. A live run can use real funds, and execution or fills are not guaranteed. Signing in and adding funds does not start a run. If the canvas has an active run, editing is locked until that run is no longer active.

## Continue learning

- [Strategy templates](/strategy-builder/templates)
- [Build your first strategy](/quickstart)
- [Strategy builder nodes](/strategy-builder/nodes)
- [Markets and outcomes](/strategy-builder/markets)
- [Conditions](/strategy-builder/conditions)
- [Actions](/strategy-builder/actions)
